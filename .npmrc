# `pnpm --filter @ss/server deploy` (the installer bundle, 18-installer-ops.md)
# requires injected workspace packages so @ss/shared is vendored into the
# self-contained bundle rather than left as a workspace symlink.
inject-workspace-packages=true
